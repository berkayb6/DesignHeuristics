import { HEURISTICS } from '../shared/heuristics';
import * as ActionTypes from './ActionTypes';

export const Heuristics = (state= HEURISTICS, action) => {
    switch (action.type){
        default:
            
            return state;
    }
}